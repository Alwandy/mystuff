var chai = require('chai');
var expect = chai.expect; // we are using the "expect" style of Chai
var CartSummary = require('../api/test-controller');

describe('testApi', function() {
  it('the test should work', function() {
    expect(0).to.equal(0);
  });
});